[project]
name = "watchlist"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "curl-cffi>=0.11.4",
    "dotenv>=0.9.9",
    "slack-sdk>=3.35.0",
]
